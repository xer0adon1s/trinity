$ trinity init
Trinity DB ready. Seeded 0 new KB entries, 0 new command explanations, and 0 new
error patterns.

[exit 0]

$ trinity parse-nmap /home/alexander/Work/trinity-wt-coverage-sim-batch3/test/fixtures/coverage_sim/previse.xml --box previse --target 10.10.11.104
──────────── 10.10.11.104:22 ssh (OpenSSH 7.6p1 Ubuntu 4ubuntu0.3) ─────────────
  OpenSSH 2.3 < 7.7 - Username Enumeration (PoC)  [MEDIUM]  (score 0.9, 
searchsploit)
  Found in local ExploitDB (EDB-ID 45210), CVE-2018-15473. Verified working.
  PoC: /usr/share/exploitdb/exploits/linux/remote/45210.py

  OpenSSH 2.3 < 7.7 - Username Enumeration  [MEDIUM]  (score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 45233), CVE-2018-15473. Verified working.
  PoC: /usr/share/exploitdb/exploits/linux/remote/45233.py

  SSH version banner grabbing for known CVEs  [INFO]  (score 0.9, user_curated)
  Old OpenSSH versions occasionally have known auth-bypass or info-leak CVEs. 
Check the exact version against searchsploit before assuming SSH is a dead end.
  Try: `searchsploit openssh <version>`

  OpenSSH < 7.7 - User Enumeration (2)  [MEDIUM]  (score 0.8, searchsploit)
  Found in local ExploitDB (EDB-ID 45939), CVE-2018-15473.
  PoC: /usr/share/exploitdb/exploits/linux/remote/45939.py

────────────────── 10.10.11.104:80 http (Apache httpd 2.4.29) ──────────────────
  Apache + PHP < 5.3.12 / < 5.4.2 - cgi-bin Remote Code Execution  [CRITICAL]  
(score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 29290), 
CVE-2012-2336;CVE-2012-2311;CVE-2012-1823;OSVDB-81633. Verified working.
  PoC: /usr/share/exploitdb/exploits/php/remote/29290.c

  Apache CXF < 2.5.10/2.6.7/2.7.4 - Denial of Service  [LOW]  (score 0.9, 
searchsploit)
  Found in local ExploitDB (EDB-ID 26710), CVE-2013-2160;OSVDB-95011. Verified 
working.
  PoC: /usr/share/exploitdb/exploits/multiple/dos/26710.txt

  Apache mod_ssl < 2.8.7 OpenSSL - 'OpenFuck.c' Remote Buffer Overflow  [HIGH]  
(score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 21671), CVE-2002-0082;OSVDB-857. Verified 
working.
  PoC: /usr/share/exploitdb/exploits/unix/remote/21671.c

  Apache mod_ssl < 2.8.7 OpenSSL - 'OpenFuckV2.c' Remote Buffer Overflow (1)  
[HIGH]  (score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 764), CVE-2002-0082;OSVDB-857. Verified 
working.
  PoC: /usr/share/exploitdb/exploits/unix/remote/764.c

  Apache Tomcat < 5.5.17 - Remote Directory Listing  [MEDIUM]  (score 0.9, 
searchsploit)
  Found in local ExploitDB (EDB-ID 2061). Verified working.
  PoC: /usr/share/exploitdb/exploits/multiple/remote/2061.txt

─────────────────────────── Suggested next commands ────────────────────────────
  searchsploit openssh 7.6p1 Ubuntu 4ubuntu0.3
  (run `trinity explain "searchsploit openssh 7.6p1 Ubuntu 4ubuntu0.3"` to break
this down)
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  (run `trinity explain "gobuster dir -u http://$TARGET:80 -w 
/usr/share/wordlists/dirb/common.txt -x php,txt,html"` to break this down)

[exit 0]

$ trinity parse-nmap /home/alexander/Work/trinity-wt-coverage-sim-batch3/test/fixtures/coverage_sim/previse_gobuster.txt --box previse
───────────────────────────────── None:None ? ──────────────────────────────────
  Anonymous FTP login  [MEDIUM]  (score 0.6, user_curated)
  FTP server allows the 'anonymous' user with any/blank password. Always worth 
checking before anything else on port 21.
  Try: `ftp <target>` then username `anonymous`, any password.

  HTTP directory brute-forcing is next after a webserver is found  [INFO]  
(score 0.6, user_curated)
  Any open HTTP/HTTPS port warrants gobuster/ffuf directory and vhost 
brute-forcing before anything else — hidden admin panels, backup files, and API 
routes are extremely common.
  Try: `gobuster dir -u http://<target> -w /usr/share/wordlists/dirb/common.txt 
-x php,txt,html`

───────────────────────────────── None:None ? ──────────────────────────────────
  Kerberos open — worth checking for AS-REP roastable accounts  [MEDIUM]  (score
0.6, user_curated)
  An open Kerberos port does NOT by itself mean any account has 'Do not require 
Kerberos preauthentication' set — most domains have zero such accounts. This is 
worth checking, not something already confirmed. IF a roastable account exists, 
GetNPUsers.py will hand back a $krb5asrep$ blob encrypted to that account's 
password with no credentials needed first — a per-account misconfig, not a 
product CVE, so there's nothing for searchsploit to find either way.
  Check with: `GetNPUsers.py <realm>/ -usersfile <users.txt> -no-pass -dc-ip 
<target>`. An empty/error result means no roastable accounts exist on this 
domain — that's a normal, common outcome, not a sign the check failed. If it 
DOES return a hash, crack offline with hashcat -m 18200. No hash is stored by 
Trinity; run the cracker yourself.

  HTTP directory brute-forcing is next after a webserver is found  [INFO]  
(score 0.6, user_curated)
  Any open HTTP/HTTPS port warrants gobuster/ffuf directory and vhost 
brute-forcing before anything else — hidden admin panels, backup files, and API 
routes are extremely common.
  Try: `gobuster dir -u http://<target> -w /usr/share/wordlists/dirb/common.txt 
-x php,txt,html`

───────────────────────────────── None:None ? ──────────────────────────────────
  HTTP directory brute-forcing is next after a webserver is found  [INFO]  
(score 0.6, user_curated)
  Any open HTTP/HTTPS port warrants gobuster/ffuf directory and vhost 
brute-forcing before anything else — hidden admin panels, backup files, and API 
routes are extremely common.
  Try: `gobuster dir -u http://<target> -w /usr/share/wordlists/dirb/common.txt 
-x php,txt,html`

───────────────────────────────── None:None ? ──────────────────────────────────
  HTTP directory brute-forcing is next after a webserver is found  [INFO]  
(score 0.6, user_curated)
  Any open HTTP/HTTPS port warrants gobuster/ffuf directory and vhost 
brute-forcing before anything else — hidden admin panels, backup files, and API 
routes are extremely common.
  Try: `gobuster dir -u http://<target> -w /usr/share/wordlists/dirb/common.txt 
-x php,txt,html`

───────────────────────────────── None:None ? ──────────────────────────────────
  HTTP directory brute-forcing is next after a webserver is found  [INFO]  
(score 0.6, user_curated)
  Any open HTTP/HTTPS port warrants gobuster/ffuf directory and vhost 
brute-forcing before anything else — hidden admin panels, backup files, and API 
routes are extremely common.
  Try: `gobuster dir -u http://<target> -w /usr/share/wordlists/dirb/common.txt 
-x php,txt,html`

─────────────────────────── Suggested next commands ────────────────────────────
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  (run `trinity explain "gobuster dir -u http://$TARGET:80 -w 
/usr/share/wordlists/dirb/common.txt -x php,txt,html"` to break this down)
  curl -i http://$TARGET/login.php
  (run `trinity explain "curl -i http://$TARGET/login.php"` to break this down)

[exit 0]

$ trinity next --box previse
─────────────────────────────── Recommended next ───────────────────────────────
searchsploit openssh 7.6p1 Ubuntu 4ubuntu0.3

You already know the exact software and version. That's specific enough to check
against a real exploit database, not guess. Port 22 is SSH — checking the exact 
version against the local exploit database is worth doing early, even though SSH
itself is rarely the first foothold.

(run `trinity explain "searchsploit openssh 7.6p1 Ubuntu 4ubuntu0.3"` for a 
command breakdown, or `trinity hint --box "previse"` if you want to work it out 
yourself first)
(done? `trinity did --box "previse"`  ·  skipping this one? `trinity skip --box 
"previse"`)

────────────────────────────── Also worth trying ───────────────────────────────
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  curl -i http://$TARGET/login.php

[exit 0]

$ trinity suggest --box previse
gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
Port 80 is serving HTTP — directory brute-forcing is the standard next step to 
find hidden admin panels, backups, or API routes before anything else.

curl -i http://$TARGET/login.php
'/login.php' (status 200) looks like an admin/login/backup-shaped path, not just
a generic hit — open it, look at any forms, and try default credentials before 
brute-forcing more paths.


[exit 0]

$ trinity hint --box previse
───────────────────────────────── Nudge (1/3) ──────────────────────────────────
You're at the recon phase. Before I just tell you what to do — look at what 
you've found so far. What's unusual, unexpected, or different about it compared 
to a typical box? What would you check first?

(ask again for a stronger hint on this same step)

[exit 0]


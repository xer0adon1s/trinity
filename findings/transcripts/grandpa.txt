$ trinity init
Trinity DB ready. Seeded 0 new KB entries, 0 new command explanations, and 0 new
error patterns.

[exit 0]

$ trinity parse-nmap /home/alexander/Work/trinity-wt-coverage-sim/test/fixtures/coverage_sim/grandpa.xml --box grandpa --target 10.10.10.14
──────────────── 10.10.10.14:80 http (Microsoft IIS httpd 6.0) ─────────────────
  Microsoft IIS 4.0/5.0/6.0 - Internal IP Address/Internal Network Name 
Disclosure  [MEDIUM]  (score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 21057), OSVDB-86899. Verified working.
  PoC: /usr/share/exploitdb/exploits/windows/remote/21057.txt

  Microsoft IIS 5.0/6.0 FTP Server (Windows 2000) - Remote Stack Overflow  
[MEDIUM]  (score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 9541), OSVDB-57589;CVE-2009-3023. Verified 
working.
  PoC: /usr/share/exploitdb/exploits/windows/remote/9541.pl

  Microsoft IIS 5.0/6.0 FTP Server - Stack Exhaustion Denial of Service  [LOW]  
(score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 9587), CVE-2009-2521;OSVDB-57753. Verified 
working.
  PoC: /usr/share/exploitdb/exploits/windows/dos/9587.txt

  Microsoft IIS 6.0 - '/AUX / '.aspx' Remote Denial of Service  [LOW]  (score 
0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 3965). Verified working.
  PoC: /usr/share/exploitdb/exploits/windows/dos/3965.pl

  Microsoft IIS 6.0 - ASP Stack Overflow Stack Exhaustion (Denial of Service) 
(MS10-065)  [LOW]  (score 0.9, searchsploit)
  Found in local ExploitDB (EDB-ID 15167), CVE-2010-1899;OSVDB-67978;MS10-065. 
Verified working.
  PoC: /usr/share/exploitdb/exploits/windows/dos/15167.txt

─────────────────────────── Suggested next commands ────────────────────────────
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  (run `trinity explain "gobuster dir -u http://$TARGET:80 -w 
/usr/share/wordlists/dirb/common.txt -x php,txt,html"` to break this down)

[exit 0]

$ trinity next --box grandpa
─────────────────────────────── Recommended next ───────────────────────────────
gobuster dir -u http://$TARGET:80 -w 
/usr/share/seclists/Discovery/Web-Content/common.txt -x php,txt,html

You have a website and you haven't looked inside it. Easy boxes hide the door in
a path, not on the port. Port 80 is serving HTTP — directory brute-forcing is 
the standard next step to find hidden admin panels, backups, or API routes 
before anything else.

(run `trinity explain "gobuster dir -u http://$TARGET:80 -w 
/usr/share/seclists/Discovery/Web-Content/common.txt -x php,txt,html"` for a 
command breakdown, or `trinity hint --box "grandpa"` if you want to work it out 
yourself first)
(done? `trinity did --box "grandpa"`  ·  skipping this one? `trinity skip --box 
"grandpa"`)

[exit 0]

$ trinity suggest --box grandpa
gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
Port 80 is serving HTTP — directory brute-forcing is the standard next step to 
find hidden admin panels, backups, or API routes before anything else.


[exit 0]

$ trinity hint --box grandpa
───────────────────────────────── Nudge (1/3) ──────────────────────────────────
You're at the enum phase. Before I just tell you what to do — look at what 
you've found so far. What's unusual, unexpected, or different about it compared 
to a typical box? What would you check first?

(ask again for a stronger hint on this same step)

[exit 0]


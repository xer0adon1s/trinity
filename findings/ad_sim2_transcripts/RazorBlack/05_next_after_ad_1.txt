$ trinity next --box RazorBlack
exit=0
----- stdout -----
─────────────────────────────── Recommended next ───────────────────────────────
ldapsearch -x -H ldap://$TARGET -s base namingcontexts

'ldapsearch' doesn't have install instructions in Trinity's tool registry yet --
try your system's package manager, or search for "ldapsearch install" for the 
official instructions.

Once that's installed, just run this same command again -- I'll pick up right 
where we left off: `trinity next --box "RazorBlack"`

(done? `trinity did --box "RazorBlack"`  ·  skipping this one? `trinity skip 
--box "RazorBlack"`)

────────────────────────────── Also worth trying ───────────────────────────────
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py raz0rblack.thm/ -usersfile users.txt -no-pass -dc-ip $TARGET
  ldapsearch -x -H ldap://$TARGET -s base namingcontexts  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py raz0rblack.thm/ -usersfile users.txt -no-pass -dc-ip $TARGET
  ldapsearch -x -H ldap://$TARGET -s base namingcontexts  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py raz0rblack.thm/ -usersfile users.txt -no-pass -dc-ip $TARGET
  enum4linux-ng -A $TARGET  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)

----- stderr -----


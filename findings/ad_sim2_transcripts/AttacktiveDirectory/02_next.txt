$ trinity next --box AttacktiveDirectory
exit=0
----- stdout -----
─────────────────────────────── Recommended next ───────────────────────────────
ldapsearch -x -H ldap://$TARGET -s base namingcontexts

'ldapsearch' doesn't have install instructions in Trinity's tool registry yet --
try your system's package manager, or search for "ldapsearch install" for the 
official instructions.

Once that's installed, just run this same command again -- I'll pick up right 
where we left off: `trinity next --box "AttacktiveDirectory"`

(done? `trinity did --box "AttacktiveDirectory"`  ·  skipping this one? `trinity
skip --box "AttacktiveDirectory"`)

────────────────────────────── Also worth trying ───────────────────────────────
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py spookysec.local/ -usersfile users.txt -no-pass -dc-ip $TARGET
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  enum4linux-ng -A $TARGET  (tool not installed)

----- stderr -----


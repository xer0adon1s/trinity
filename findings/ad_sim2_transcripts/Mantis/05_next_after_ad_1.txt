$ trinity next --box Mantis
exit=0
----- stdout -----
─────────────────────────────── Recommended next ───────────────────────────────
ldapsearch -x -H ldap://$TARGET -s base namingcontexts

'ldapsearch' doesn't have install instructions in Trinity's tool registry yet --
try your system's package manager, or search for "ldapsearch install" for the 
official instructions.

Once that's installed, just run this same command again -- I'll pick up right 
where we left off: `trinity next --box "Mantis"`

(done? `trinity did --box "Mantis"`  ·  skipping this one? `trinity skip --box 
"Mantis"`)

────────────────────────────── Also worth trying ───────────────────────────────
  enum4linux-ng -A $TARGET  (tool not installed)
  ldapsearch -x -H ldap://$TARGET -s base namingcontexts  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  gobuster dir -u http://$TARGET:1337 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  gobuster dir -u http://$TARGET:1337 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  gobuster dir -u http://$TARGET:8080 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  gobuster dir -u http://$TARGET:8080 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py htb.local/ -usersfile users.txt -no-pass -dc-ip $TARGET
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py htb.local/ -usersfile users.txt -no-pass -dc-ip $TARGET

----- stderr -----


$ trinity next --box Fuse
exit=0
----- stdout -----
─────────────────────────────── Recommended next ───────────────────────────────
ldapsearch -x -H ldap://$TARGET -b 'DC=fabricorp,DC=local' '(objectClass=user)' 
sAMAccountName

'ldapsearch' doesn't have install instructions in Trinity's tool registry yet --
try your system's package manager, or search for "ldapsearch install" for the 
official instructions.

Once that's installed, just run this same command again -- I'll pick up right 
where we left off: `trinity next --box "Fuse"`

Most of the recent timeline is still in `enum` while other leads sit untouched. 
the classic trap is deciding one port *must* be the answer. parking it for a bit
is allowed..

(done? `trinity did --box "Fuse"`  ·  skipping this one? `trinity skip --box 
"Fuse"`)

────────────────────────────── Also worth trying ───────────────────────────────
  ldapsearch -x -H ldap://$TARGET -s base namingcontexts  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py fabricorp.local/ -usersfile users.txt -no-pass -dc-ip $TARGET
  ldapsearch -x -H ldap://$TARGET -s base namingcontexts  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py fabricorp.local/ -usersfile users.txt -no-pass -dc-ip $TARGET
  ldapsearch -x -H ldap://$TARGET -s base namingcontexts  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  GetNPUsers.py fabricorp.local/ -usersfile users.txt -no-pass -dc-ip $TARGET
  gobuster dir -u http://$TARGET:5985 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  gobuster dir -u http://$TARGET:5985 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  gobuster dir -u http://$TARGET:5985 -w /usr/share/wordlists/dirb/common.txt -x
php,txt,html
  gobuster dir -u http://$TARGET:80 -w /usr/share/wordlists/dirb/common.txt -x 
php,txt,html
  enum4linux-ng -A $TARGET  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)
  enum4linux-ng -A $TARGET  (tool not installed)

----- stderr -----

